# Python виртуална среда — пресъздава се от requirements.txt, не се качва
.venv/
__pycache__/
**/__pycache__/ls 
*.pyc

# Базата с реални данни — съдържанието не е код, не е за версиониране
bookspace.db
bookspace.db.backup_*
*.db.before_*

# Локални настройки на редактора, ОС
.vscode/
.idea/
.DS_Store
Thumbs.db

# Streamlit временни файлове
.streamlit/secrets.toml

# ТАЙНИ — НИКОГА в git. Пароли, JWT ключ, SMTP/API ключове.
.env
.env.local
backend/.env
backend/.env.local
*.pem
*.key

# Логове от изпълнение — не са код, не се версионират
streamlit_log.txt
*.log

# Прикачени фактури (снимки/PDF) — потребителски данни, не са код
uploaded_invoices/